(ns instrumentation-clj.instrumentation-clj-test
  (:require [clojure.test :refer :all]
            [instrumentation-clj.instrumentation-clj :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
